jQuery(document).ready(function($){
	//show metaboxes for specific page templates then the template is selected
	$('#page_template').change(function() {
		var selectVal = $(this).val();
		// $('#*_options').toggle( selectVal == 'page-templates/*-template.php' );
		
	}).change();

});